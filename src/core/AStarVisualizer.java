package src.core;

import src.ui.MainFrame;

/**
 * Backward-compatibility entry point — delegates to MainFrame.
 */
public class AStarVisualizer {
    public static void main(String[] args) {
        MainFrame.main(args);
    }
}
